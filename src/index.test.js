'use strict';
var expect = chai.expect;

describe('the koast angular mobile app', function () {
  beforeEach(module('koast.mobile'));

  it('should have an instance of the mobile app', function () {
    expect(1).to.be.equal(1);
  });
});
